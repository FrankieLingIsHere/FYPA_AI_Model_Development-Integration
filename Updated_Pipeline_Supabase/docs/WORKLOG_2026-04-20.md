# Worklog - 2026-04-20

## Scope
1. Harden reconnect/offline validation reliability.
2. Prevent overlapping Playwright runs from corrupting output artifacts.
3. Make Supabase DB duplicate inserts idempotent and prevent aborted-transaction cascades.
4. Validate strict local/offline scenario and proceed with commit/sync only on pass.

## Code Changes

### 1) Playwright single-run guard and reused-backend diagnostics
File:
- tmp_ui_real_dual_report_playwright.py

Changes:
1. Added lockfile-based single-run guard (`tmp_ui_real_dual_report_playwright.lock`) to fail fast when another run is active.
2. Added stale-lock recovery (dead PID or aged lock) and safe lock release logic.
3. Preserved run-lock metadata in JSON output (`run_lock`).
4. Improved reused-backend diagnostics so runtime/cloud resolution fields are still populated.
5. Provisioning bootstrap now falls back to resolved deployed API base URL when `cloud_url_override` is absent.

### 2) Supabase DB duplicate-idempotency and rollback safety
File:
- Updated_Pipeline_Supabase/pipeline/backend/core/supabase_db.py

Changes:
1. Added unique-constraint detection helper for duplicate key violations.
2. Added existing-row lookup helpers for:
   - `detection_events.report_id`
   - `violations.id` by `report_id`
3. `insert_detection_event` now treats duplicate insert as idempotent success by returning existing report id.
4. `insert_violation` now treats duplicate insert as idempotent success by returning existing violation id.
5. Added `_safe_rollback()` in read/query exception paths (`get_violation`, `get_recent_violations`, `get_detection_event`, `get_recent_detection_events`, `get_all_violations_with_status`, `get_device_stats`, `get_recent_logs`) to prevent `current transaction is aborted` cascades.

## Offline Validation Evidence

Command executed:
1. `python .agents/skills/local-mode-playwright-validation/run_playwright_validation.py --scenario local-reconnect --check-local-backend --local-ui-url http://127.0.0.1:5000 --json-out tmp_local_reconnect_offline_result.json`

Result summary:
1. `all_passed=true`
2. `results[0].script=Updated_Pipeline_Supabase/local_mode_ui_checkup_reconnect_perf_test.py`
3. `results[0].status=PASS`
4. `results[0].exit_code=0`
5. `results[0].payload_pass=true`

Required metrics:
1. `routing_snapshot.before_offline=local`
2. `routing_snapshot.after_offline=local`
3. `routing_snapshot.after_reconnect=cloud`
4. `sync_started_auto_after_reconnect=true`
5. `sync_seen_auto_after_reconnect=true`
6. `sync_seen_after_reconnect=true`
7. `manual_sync_attempt.attempted=false`
8. `sync_local_cache_summary.calls=5`
9. `sync_local_cache_summary.success_calls=0`
10. `sync_local_cache_summary.avg_duration_ms=552.14`
11. `sync_local_cache_summary.max_duration_ms=2712`
12. `sync_after_reconnect_started_calls_len=3`
13. `sync_after_reconnect_calls_len=3`

Offline decision gate:
1. `STEP6_OFFLINE_LOCAL_REPORT_GENERATION_PASSED=True (all_passed=True, payload_pass=True)`

## Git Activity
1. Pending (updated after commit/sync in this same session).

## Artifact Cleanup
1. Pending (performed after commit/sync in this same session).