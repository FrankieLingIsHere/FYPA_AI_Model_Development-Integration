# Worklog - 2026-05-13

- Installed additional local skill bundles for frontend review support:
  - `figma-generate-design`
  - `playwright`
  - `screenshot`
- Checked the current skill catalog for animation-generation support and did not find a matching installable animation skill in the available upstream set.
- Reworked the desktop sidebar so labels stay visible in the normal state instead of only on hover, while keeping the same page shell and interaction model.
- Shortened timezone selector display labels and widened the desktop sidebar treatment so the active timezone no longer clips in the shell.
- Normalized the desktop sidebar voice controls so both buttons render with readable text and a consistent finish.
- Updated the mobile bottom navigation so each destination keeps a visible text label instead of collapsing to icons only.
- Replaced the mobile handbook pill-strip navigation with the existing handbook section picker to avoid truncation during rapid touch interactions.
- Added an interactive tutorial section to the handbook workflow page with:
  - cloud-mode walkthrough
  - local-mode walkthrough
  - step-by-step mode
  - autoplay demo mode
  - switching cautions and operator reminders
- Expanded the analytics page with more meaningful metrics:
  - ready rate
  - high-severity share
  - local-origin vs cloud-origin mix
  - 7-day daily average
  - peak monitoring window
  - dominant source mix
  - refreshed insight card styling
- Browser-verified the updated shell on desktop and mobile with Playwright screenshots and DOM checks:
  - desktop sidebar labels visible
  - desktop timezone selector no longer overflows
  - mobile handbook section control visible
  - mobile handbook nav strip no longer used on phones
  - mobile bottom-nav labels visible again
- Measured current report timing probes:
  - cloud failed/no-report reprocess sample `20260508_160249` completed in about `26.32s`
  - local backend sample `20260430_120230` ran for about `133.78s` and still failed on the sampled `Local Synced` path
- Re-ran required action tests before push:
  - `deployed_frontend_navigation_timezone_action_test.py` passed against the patched local frontend
  - `deployed_provisioning_action_test.py` passed

- Restored the desktop sidebar hover-collapse behavior so the shell returns to icon-only width when idle, while still expanding for hover, timezone focus, and quick cursor movement.
- Redesigned the sidebar voice alert controls with structured label/state copy so the control cluster looks intentional in both collapsed and expanded desktop states.
- Hardened report-source tagging so cloud-origin reports stay `Cloud`, unsynced local-origin reports stay `Local`, and only real local-sync evidence promotes a report to `Local Synced`.
- Removed a sync-event fallback that could rewrite cloud report IDs to `Local Synced` even when no strict local-origin evidence existed.
- Tightened backend merge paths so local staging artifacts no longer upgrade cloud report rows to `Local Synced` in `/api/violations` or stats merges.
- Added new regression coverage for:
  - cloud reports ignoring local-sync events
  - cloud reports staying `Cloud` while local staging files exist
  - unsynced local reports repairing back to `Local`
  - local reports promoting to `Local Synced` only after reconnect/sync evidence appears
- Re-ran required action gates on the patched frontend/backend:
  - `deployed_frontend_navigation_timezone_action_test.py` passed against `http://127.0.0.1:5001`
  - `deployed_provisioning_action_test.py` passed
- Used `python-performance-optimization` to benchmark the Local Mode Checkup/API path:
  - local host `GET /api/reports/recovery/options?checkup_only=1`: avg about `2088.75ms`
  - local host `GET /api/local-mode/provisioning/status`: avg about `979.41ms`
  - local host safe `POST /api/local-mode/prepare`: avg about `8271.61ms`
  - deployed cloud/admin-side `GET /api/reports/recovery/options?checkup_only=1`: avg about `155.09ms`
  - deployed cloud/admin-side `GET /api/local-mode/provisioning/status`: avg about `164.43ms`
- Profiled the local `checkup_only` path and confirmed the dominant cost is `_get_local_mode_diagnostics()` calling the local runtime/Ollama readiness probe, not the admin/provisioning status code itself.
- Used `local-mode-playwright-validation` to run the reconnect stability flow against the patched local app:
  - `all_passed: true`
  - preflight root/startup-status/queue-status checks all `ok`
  - `sync_started_auto_after_reconnect: true`
  - `sync_seen_auto_after_reconnect: true`
  - `sync_seen_after_reconnect: true`
  - `manual_sync_attempt.attempted: false`
  - `sync_local_cache_summary.calls: 6`
  - `sync_local_cache_summary.success_calls: 1`
  - `sync_local_cache_summary.avg_duration_ms: 19147.75`
  - `sync_local_cache_summary.max_duration_ms: 30090.5`

- Added a new fixed in-product `CASM Assistant` shell inspired by AskBo, available on every page through a floating launcher that stays clear of the main notification bell and mobile bottom navigation.
- Kept the assistant lightweight on purpose by building it as a client-side natural-language helper instead of adding a new always-on backend chat service.
- Implemented assistant capabilities:
  - global page shortcuts (`Home`, `Live`, `Reports`, `Analytics`, `Settings`, `Manual`)
  - handbook/documentation search with natural-language filtering
  - tutorial steps rendered directly inside chat for both `Cloud` and `Local` flows
  - chat-side tutorial stepping (`Previous`, `Next`, `Open in handbook`)
  - CSV export helpers for reports and analytics
  - locally persisted session history so users can return to prior assistant conversations
  - lightweight per-session memory for tutorial position, recent pages, last docs query, and last export type
- Exposed the existing handbook/tutorial data through reusable frontend hooks so the assistant and the handbook stay aligned instead of drifting into separate copies.
- Added a router route-change event so the assistant can remember navigation history and stay aware of the current shell context.
- Added a focused browser action test for the assistant covering:
  - desktop launcher placement
  - notification-stack offset while assistant is open
  - chat tutorial rendering and step advancement
  - CSV download trigger
  - handbook opening from documentation search results
  - session creation/history
  - mobile launcher clearance above bottom nav
- Fixed an intent-routing bug discovered during testing where `find docs about ...` incorrectly opened the `About` page because `about` matched too broadly in shortcut detection.
- Adjusted the mobile assistant dock offset after live browser measurement so the launcher no longer overlaps the mobile bottom navigation by a few pixels.
- Re-ran browser validation on the already-healthy local app instance at `http://127.0.0.1:5000`:
  - `deployed_frontend_assistant_action_test.py` passed
  - `deployed_frontend_navigation_timezone_action_test.py` passed
  - `deployed_frontend_mobile_usability_test.py` passed
- Re-ran syntax checks on updated shared frontend scripts:
  - `frontend/js/assistant.js`
  - `frontend/js/app.js`
  - `frontend/js/router.js`

- Tightened the assistant shell so notification toasts no longer cover the open chat panel:
  - desktop notification stack shifts left and below the main workspace while the assistant is open
  - mobile notification stack stays clear of the open assistant panel
  - open assistant panel remains above toast z-order if a toast still appears near it
- Made the assistant input area more self-explanatory:
  - normalized the chat input placeholder with clean prompt examples
  - added a visible "Type here" guide
  - added a starter prompt deck for first-time users
  - clarified in the assistant copy that synced session history is admin-only
- Extended the assistant's local/browser memory and admin visibility boundary:
  - per-browser client identity and private sync secret
  - synced assistant sessions stored in a local admin-only log endpoint/view
  - `/admin/assistant-sessions` now supports admin review while unauthenticated reads stay blocked
- Real-tested assistant export and natural-language filtering against the patched local app on `http://127.0.0.1:5002`:
  - analytics export CSV downloaded and included `ready_rate_percent`
  - reports export prompt `export high local synced reports csv` downloaded successfully
  - exported report rows all matched the requested filters (`source_scope=synced_local`, `severity=high`)
  - handbook search prompt `find docs about wifi reconnect local sync` opened the expected workflow/manual content
- Strengthened the browser action test for the assistant to verify:
  - assistant input and prompt deck visibility
  - toast/panel non-overlap on desktop and mobile
  - tutorial rendering and step advancement in chat
  - actual analytics CSV content
  - actual natural-language-filtered report CSV content
  - assistant session creation/history
  - admin-only assistant session sync readback, including unauthenticated denial
- Re-ran local browser validation against the fresh patched app instance on `http://127.0.0.1:5002`:
  - `deployed_frontend_assistant_action_test.py` passed
  - `deployed_frontend_navigation_timezone_action_test.py` passed
  - `deployed_frontend_mobile_usability_test.py` passed
- Added local-only ignore rules for generated assistant session log files and assistant server logs so runtime artifacts do not get staged later by accident.
- Reworked the assistant prompt chips from a fixed starter list into adaptive follow-up suggestions driven by current session context:
  - starter mode for first-use
  - `tutorial-local` and `tutorial-cloud` follow-up prompts during walkthroughs
  - docs-search follow-up prompts after handbook/manual queries
  - analytics-export and reports-export follow-up prompts after CSV downloads
  - route-aware/local-readiness/report-review/live-monitor follow-up prompts based on recent page and last request
- Added regression coverage to assert prompt-chip state transitions through:
  - starter prompts
  - local tutorial prompts
  - analytics export prompts
  - docs-search prompts
- Ran a browser-based live demo on the patched app at `http://127.0.0.1:5002` and captured screenshots plus timings for:
  - assistant open
  - local tutorial load
  - tutorial next-step advance
  - docs search
  - handbook open from assistant result
  - analytics CSV export
  - filtered reports CSV export
- Demo timing sample from the final run:
  - assistant open: about `433ms`
  - local tutorial response: about `404ms`
  - tutorial next-step advance: about `424ms`
  - docs search response: about `389ms`
  - handbook jump: about `685ms`
  - analytics CSV export: about `4528ms`
  - filtered reports CSV export: about `496ms`
- Refined prompt-chip labels to keep the adaptive row readable without earlier clipping pressure on narrower desktop widths.
- Promoted the assistant from a utility panel into a more explicit chatbot identity:
  - named the assistant `Mira`
  - added the `CASM Safety Copilot` header treatment
  - updated launcher/input labels so the UI reads like a chat entry point rather than a generic helper
- Fixed a serious laptop layout bug where the assistant looked "closed" but still occupied layout space because `.assistant-panel` overrode the shared `.hidden` class with `display: flex`.
- Reworked the chat body so the conversation becomes the true flexible scroll region:
  - corrected the assistant main grid row structure
  - constrained the panel shell height
  - kept long transcripts scrollable inside the chat instead of clipping the panel
  - added desktop wheel relaying so scrolling over prompt chips or other panel chrome still moves the chat transcript naturally
- Restyled messages into a clearer chatbot presentation with speaker name, avatar, and bubble grouping for both the assistant and the user.
- Strengthened the assistant browser action test to cover the newly discovered failure mode on a real laptop-sized viewport:
  - verifies the launcher no longer overlaps the notification bell before opening
  - verifies the assistant title shows `Mira`
  - verifies the transcript becomes genuinely scrollable after several messages
  - verifies mouse-wheel scrolling over the panel moves the chat transcript
- Re-ran the real browser validation on a fresh patched local app instance at `http://127.0.0.1:5004`:
  - `deployed_frontend_assistant_action_test.py` passed
  - `deployed_frontend_navigation_timezone_action_test.py` passed
  - `deployed_frontend_mobile_usability_test.py` passed
- Captured a final responsiveness sample on the fixed assistant flow:
  - open assistant: about `79ms`
  - load local tutorial in chat: about `36ms`
  - advance to next tutorial step: about `25ms`
- Installed additional frontend-design skills locally for future assistant/design work:
  - `figma`
  - `figma-use`
  - `figma-implement-design`
- Ran a deeper assistant UX sweep focused on comfort and flow:
  - reduced the desktop assistant panel footprint to a more chat-like size instead of a wide utility slab
  - kept the dock in the bottom-right lane without colliding with the notification bell or mobile bottom nav
  - kept the launcher as a small fixed icon while the full panel stays hidden until opened
- Reworked tutorial handling into a slideshow-style guide instead of stacking a new long tutorial message for every step:
  - tutorial progress now updates inside a single guided card
  - previous/next navigation keeps the same card alive rather than extending the transcript
  - added a visible progress track so the current step is legible at a glance
- Extended Mira's operational guidance so natural-language requests can hand the user into the right workflow:
  - `help me start live monitoring` now offers a direct handoff into `Live Monitor` camera mode
  - `can you check if this image has violations` now offers a direct handoff into `Analyze Image`
  - both handoffs collapse the assistant after navigation so the workspace stays usable
  - reopening the assistant returns to the same chat/session so the user can continue from where they left off
- Added assistant-driven settings support:
  - `recommend settings` now explains the main operating profiles
  - Mira can apply `recommended`, `API mode`, or `local profile` actions from chat
  - settings application now returns structured success/failure outcomes so the assistant can acknowledge them properly
- Added a Live Monitor bridge so assistant navigation can land on the right sub-mode and focus area:
  - camera stream handoff focuses the `Start` control
  - image-analysis handoff switches to upload mode and scrolls the upload area into view
- Strengthened the assistant browser action test again to verify:
  - compact laptop-size panel bounds
  - slideshow tutorial behavior stays to one card
  - live-monitor handoff closes the assistant and preserves session continuity
  - image-analysis handoff reaches the upload workflow
  - recommended settings can be applied from chat
  - handbook handoff closes the assistant cleanly
- Re-ran the full local browser validation on the fresh patched app instance at `http://127.0.0.1:5005`:
  - `deployed_frontend_assistant_action_test.py` passed
  - `deployed_frontend_navigation_timezone_action_test.py` passed
  - `deployed_frontend_mobile_usability_test.py` passed
- Extended Mira's analytics handling so analytics questions now produce an on-the-spot filtered snapshot inside chat:
  - analytics questions render metrics directly in the assistant bubble instead of only linking away
  - the assistant can now pass the same requested filter slice into the real Analytics page
  - the Analytics page shows an active assistant-filter banner and can clear back to the full dashboard
- Added clearer low-signal and off-topic logic for unsupported prompts:
  - symbol/noise-only input gets a "usable request" recovery reply
  - coherent but unrelated prompts get an explicit scope-boundary reply
  - both fallback paths now suggest concrete next actions instead of silently failing or pretending to understand
- Added browser coverage for:
  - filtered analytics question -> chat metrics -> filtered analytics page handoff
  - low-signal input handling
  - unrelated/off-topic input handling
- Re-ran the full local browser validation on the fresh patched app instance at `http://127.0.0.1:5006`:
  - `deployed_frontend_assistant_action_test.py` passed
  - `deployed_frontend_navigation_timezone_action_test.py` passed
  - `deployed_frontend_mobile_usability_test.py` passed
- Added a first-time-user onboarding path to Mira's adaptive prompt deck:
  - starter prompts now include `I'm new here`
  - novice prompts like `i dont know what should i do first` switch the deck into a dedicated onboarding mode
  - onboarding recommendations now steer users toward live monitoring, image analysis, analytics, recommended settings, or the handbook
- Re-ran the assistant browser walkthrough on the fresh patched app instance at `http://127.0.0.1:5007`:
  - `deployed_frontend_assistant_action_test.py` passed, including the new onboarding prompt-deck assertion

- Reworked Mira's assistant panel for better chat comfort on laptop and mobile:
  - enlarged the usable chat surface while keeping it responsive
  - improved mobile panel sizing so the conversation remains readable without horizontal overflow
  - added in-chat response feedback states such as `Thinking...`, `Checking the analytics view...`, and `Preparing the export...`
- Hardened Mira's intent handling for natural wording:
  - prompts like `i wanna start supervision`, `can monitor worker now`, and `please watch site camera` now resolve to live monitoring
  - image prompts like `check this photo for ppe` and `scan picture violation` resolve to image-analysis mode
  - explanation prompts such as `what is local mode` now explain the concept instead of accidentally applying a settings profile
- Added a local, non-LLM intent-understanding layer to reduce latency and cost:
  - example-based intent scoring across live monitoring, image analysis, analytics, reports, exports, settings, tutorials, docs, and onboarding
  - typo/slang normalization for terms like `wanna`, `anlytics`, `stats`, `photo`, and `supervision`
  - structured entity extraction for analytics filters, PPE types, page targets, export kind, live/upload mode, and settings profile
  - confidence thresholds for direct action, clarification, and safe fallback
  - local unmatched/low-confidence prompt logging in browser storage for future review
- Strengthened analytics prompt handling:
  - assistant filters are sanitized before reaching the Analytics page
  - invalid/free-text filters are ignored instead of creating empty or truncated filtered dashboards
  - PPE filter extraction now supports helmet/hardhat, vest, gloves, mask, goggles, and safety shoes/boots
- Upgraded the Analytics page violation overview from static metric cards into an interactive overview panel:
  - `Risk`, `Source`, `Queue`, and `Timing` focus tabs
  - ready-rate ring, severity mix, source stack, timing bars, and mini summary counters
  - quick actions for reports/live monitor and CSV export
  - fallback severity/breakdown derivation from violation rows when backend stats are incomplete
- Verified the new assistant/NLU/analytics behavior with local syntax and browser checks:
  - `node --check frontend/js/assistant.js` passed
  - `node --check frontend/js/pages/analytics.js` passed
  - `git diff --check` passed
  - Playwright smoke test confirmed analytics overview tab switching, assistant response feedback, readable desktop/mobile panel sizing, and no horizontal overflow
  - Prompt matrix confirmed messy prompts route without adding individual regex edge cases
  - Browser chat test confirmed:
    - `show me high helmet issue this week` produced a filtered analytics snapshot
    - `what is local mode` produced an explanation
    - unrelated input fell back safely with concrete action suggestions
