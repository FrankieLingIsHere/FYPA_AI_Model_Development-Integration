# Worklog - 2026-05-14

- Used `assistant_prompt_ideas.md` as the assistant prompt-coverage source for this pass, with focus on messy wording, combined requests, privacy/permission-aware prompts, and random/off-topic recovery.
- Strengthened Mira's local, non-LLM prompt handling:
  - added a privacy/security guardrail before workflow routing so requests for secrets, tokens, sync secrets, credentials, cross-user sessions, or permission bypasses are refused safely
  - added a privacy-safe guidance path for prompts asking what can be shared, anonymized, redacted, or shown without exposing sensitive data
  - added compound-request detection so prompts like camera check + violation summary are split into several safe workspace actions instead of losing intent
  - added capability/limitation responses that explain Mira's scope and permission boundaries
  - expanded typo, plural, slang, and code-switched normalization for prompts such as `wats the statuse of the camra`, `boleh check camera status`, and `show me teh violashuns`
- Improved analytics/report filter extraction:
  - added `yesterday` as a valid assistant date filter
  - passed `yesterday` through the Analytics page sanitizer and row-matching logic
  - kept unsupported/free-text filters ignored so invalid prompt fragments do not create broken or truncated analytics views
- Updated Live Monitor image analysis to support multiple image uploads:
  - enabled multi-select on the image input
  - added drag/drop handling for multiple files
  - added a preview grid and selected-file summary
  - queued each selected image through the existing single-image inference endpoint
  - rendered aggregate detection counts plus per-image detection/annotation results
- Verified the changes:
  - `node --check Updated_Pipeline_Supabase/frontend/js/assistant.js`
  - `node --check Updated_Pipeline_Supabase/frontend/js/pages/live.js`
  - `node --check Updated_Pipeline_Supabase/frontend/js/pages/analytics.js`
  - `git diff --check`
  - deterministic prompt matrix covering supervision wording, camera typos, yesterday violation filters, combined camera/violation prompts, privacy prompts, secret/token refusal, code-switched camera status, and CSV export wording
  - Playwright browser smoke against a local static/mock API server confirmed assistant secret refusal, combined-request action splitting, multi-image selection, two-image preview rendering, and two-image upload analysis completion
